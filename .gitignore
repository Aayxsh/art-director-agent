# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
*.key

# Model weights & generated artifacts — never commit these
*.safetensors
*.ckpt
models/
outputs/
generated_images/
eval/results/*.json
# ...except the committed benchmark artifacts backing README.md's Results
# table (docs/EVAL.md) — real research data, not per-session replay logs.
!eval/results/single_shot_benchmark.json
!eval/results/full_loop_benchmark.json

# OS
.DS_Store

# Claude Code session runtime state
.claude/scheduled_tasks.lock
